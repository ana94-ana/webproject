const getters = {
    getAddButtonClasses(state) {
        return state.button_clases.add
    },
    getRemoveButtonClasses(state) {
        return state.button_clases.remove
    }

}

export default getters